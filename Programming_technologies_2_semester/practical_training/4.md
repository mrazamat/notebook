# Программа для разделения слов и предложений

**Предмет:** Технологии программирования  
**Специальность:** Компьютерная лингвистика  
Практический урок  



## Введение

Разделение текста на слова (токенизация) и предложения (сегментация) — это одна из основных задач в области обработки естественного языка (NLP). Эти процессы являются первым этапом во многих приложениях обработки текста, включая анализ тональности, машинный перевод и извлечение информации.

### Цели урока

- Понимать различие между токенизацией слов и сегментацией предложений
- Реализовать базовые алгоритмы разделения текста
- Использовать готовые библиотеки для профессиональной обработки
- Разобраться в особенностях обработки различных языков
- Решать практические задачи в контексте компьютерной лингвистики



## Часть 1: Теоретические основы

### 1.1 Токенизация (разделение на слова)

**Определение:** Токенизация — это процесс разделения текста на отдельные элементы (токены), обычно слова, пунктуацию или субслова.

#### Типы токенизации:

**Простая токенизация по пробелам:**
```
Текст: "Привет, как дела?"
Результат: ["Привет,", "как", "дела?"]
```

**Токенизация с отделением пунктуации:**
```
Текст: "Привет, как дела?"
Результат: ["Привет", ",", "как", "дела", "?"]
```

**Субсловная токенизация (BPE, WordPiece):**
```
Текст: "разговорчивый"
Результат: ["раз", "говор", "чи", "вый"]
```

### 1.2 Сегментация предложений

**Определение:** Сегментация предложений — это разделение текста на отдельные предложения по граничным маркерам (точка, восклицательный знак, вопросительный знак).

#### Сложности при сегментации:

1. **Аббревиатуры:** "Ср. статью ниже" — точка не является границей предложения
2. **Числа:** "1.5 км" — точка в числе
3. **Веб-адреса и электронная почта:** "info@example.com"
4. **Многоточие:** "Погодите... Что это?"
5. **Кавычки и скобки:** Предложение может заканчиваться внутри них

### 1.3 Лингвистические проблемы

**Русский язык:**
- Аббревиатуры: "тыс.", "т.е.", "г-н"
- Сокращения: "Петр I" (латинская цифра)
- Клитики: "-ли", "-же"

**Английский язык:**
- Притяжательные формы: "John's", "students'"
- Сокращения: "don't", "it's"
- Многосложные выражения: "New York"



## Часть 2: Практический урок

### Пример 1: Простая токенизация на Python

```python
def simple_tokenization(text):
    """
    Простая токенизация по пробелам
    
    Args:
        text (str): Исходный текст
        
    Returns:
        list: Список токенов
    """
    tokens = text.split()
    return tokens


# Пример использования
text = "Обработка естественного языка - это интересно!"
result = simple_tokenization(text)
print(result)
# Вывод: ['Обработка', 'естественного', 'языка', '-', 'это', 'интересно!']
```

### Пример 2: Токенизация с отделением пунктуации

```python
import re

def advanced_tokenization(text):
    """
    Токенизация с отделением пунктуации
    
    Args:
        text (str): Исходный текст
        
    Returns:
        list: Список токенов
    """
    # Регулярное выражение: отделяем пунктуацию
    pattern = r'(\w+|[.,!?;:\-—])'
    tokens = re.findall(pattern, text)
    return tokens


# Пример использования
text = "Компьютерная лингвистика изучает язык. Это сложно, но интересно!"
result = advanced_tokenization(text)
print(result)
# Вывод: ['Компьютерная', 'лингвистика', 'изучает', 'язык', '.', 
#         'Это', 'сложно', ',', 'но', 'интересно', '!']
```

### Пример 3: Сегментация предложений

```python
import re

def simple_sentence_segmentation(text):
    """
    Простая сегментация предложений
    
    Args:
        text (str): Исходный текст
        
    Returns:
        list: Список предложений
    """
    # Разделяем по точке, вопросительному и восклицательному знакам
    sentences = re.split(r'[.!?]+', text)
    # Удаляем пустые строки и лишние пробелы
    sentences = [s.strip() for s in sentences if s.strip()]
    return sentences


# Пример использования
text = "Первое предложение. Второе предложение? Третье!"
result = simple_sentence_segmentation(text)
for i, sent in enumerate(result, 1):
    print(f"{i}. {sent}")
# Вывод:
# 1. Первое предложение
# 2. Второе предложение
# 3. Третье
```

### Пример 4: Умная сегментация с обработкой аббревиатур

```python
import re

class SmartSentenceSegmenter:
    """
    Умная сегментация предложений с обработкой аббревиатур
    """
    
    def __init__(self):
        # Список известных аббревиатур
        self.abbreviations = {
            'тыс.', 'млн.', 'млрд.', 'т.е.', 'т.д.', 'т.п.',
            'др.', 'и т.д.', 'и т.п.', 'ср.', 'см.', 'рис.',
            'примеч.', 'авт.', 'вт.', 'ч.', 'г.', 'гр.',
            'проф.', 'г-н', 'г-жа', 'г-ж.', 'стр.', 'гл.'
        }
    
    def segment(self, text):
        """
        Разделяет текст на предложения
        
        Args:
            text (str): Исходный текст
            
        Returns:
            list: Список предложений
        """
        # Защищаем аббревиатуры временными маркерами
        protected_text = text
        markers = {}
        
        for i, abbr in enumerate(self.abbreviations):
            marker = f"__ABBR_{i}__"
            markers[marker] = abbr
            protected_text = protected_text.replace(abbr, marker)
        
        # Разделяем по пунктуации
        sentences = re.split(r'([.!?])\s+', protected_text)
        
        # Восстанавливаем аббревиатуры и формируем предложения
        result_sentences = []
        current_sentence = ""
        
        for i, part in enumerate(sentences):
            current_sentence += part
            
            # Если это пунктуация, завершаем предложение
            if part in '.!?':
                # Восстанавливаем маркеры
                for marker, abbr in markers.items():
                    current_sentence = current_sentence.replace(marker, abbr)
                
                if current_sentence.strip():
                    result_sentences.append(current_sentence.strip())
                current_sentence = ""
        
        # Добавляем последнее предложение, если оно есть
        if current_sentence.strip():
            for marker, abbr in markers.items():
                current_sentence = current_sentence.replace(marker, abbr)
            result_sentences.append(current_sentence.strip())
        
        return result_sentences


# Пример использования
segmenter = SmartSentenceSegmenter()
text = "Компания получила 1.5 млн. рублей. Это большая сумма. Что дальше?"
sentences = segmenter.segment(text)
for i, sent in enumerate(sentences, 1):
    print(f"{i}. {sent}")
```

### Пример 5: Использование NLTK для русского языка

```python
# Установка: pip install nltk

import nltk
from nltk.tokenize import word_tokenize, sent_tokenize
from nltk.corpus import stopwords

# Загрузка необходимых данных
nltk.download('punkt')
nltk.download('stopwords')

def tokenize_with_nltk(text, language='russian'):
    """
    Токенизация и сегментация с помощью NLTK
    
    Args:
        text (str): Исходный текст
        language (str): Язык текста
        
    Returns:
        tuple: (список предложений, список токенов)
    """
    # Сегментация предложений
    sentences = sent_tokenize(text, language=language)
    
    # Токенизация всего текста
    tokens = word_tokenize(text, language=language)
    
    return sentences, tokens


# Пример использования
text = """Компьютерная лингвистика изучает методы обработки текстов. 
Она использует различные алгоритмы и методы. Это перспективная область!"""

sentences, tokens = tokenize_with_nltk(text)

print("Предложения:")
for i, sent in enumerate(sentences, 1):
    print(f"{i}. {sent}")

print("\nТокены:")
print(tokens)
```

### Пример 6: Использование spaCy

```python
# Установка: pip install spacy
# Скачивание модели: python -m spacy download ru_core_news_sm

import spacy

def process_with_spacy(text):
    """
    Обработка текста с помощью spaCy
    
    Args:
        text (str): Исходный текст
        
    Returns:
        spacy Doc: Обработанный документ
    """
    # Загрузка модели русского языка
    nlp = spacy.load("ru_core_news_sm")
    
    # Обработка текста
    doc = nlp(text)
    
    return doc


# Пример использования
text = "Александр Пушкин написал \"Евгения Онегина\" в 1833 году. Это великое произведение!"

doc = process_with_spacy(text)

print("Предложения:")
for i, sent in enumerate(doc.sents, 1):
    print(f"{i}. {sent.text}")

print("\nТокены (с лемматизацией):")
for token in doc:
    print(f"{token.text:15} -> {token.lemma_:15} | POS: {token.pos_}")
```

### Пример 7: Полная программа обработки текста

```python
import re
from collections import Counter

class TextProcessor:
    """
    Полная программа для обработки текста
    """
    
    def __init__(self):
        self.text = ""
        self.sentences = []
        self.tokens = []
        self.vocabulary = Counter()
    
    def load_text(self, text):
        """Загрузить текст"""
        self.text = text
        return self
    
    def segment_sentences(self):
        """Разделить на предложения"""
        # Простое разделение по пунктуации
        pattern = r'[.!?]+'
        sentences = re.split(pattern, self.text)
        self.sentences = [s.strip() for s in sentences if s.strip()]
        return self
    
    def tokenize(self):
        """Токенизировать каждое предложение"""
        for sentence in self.sentences:
            # Отделяем пунктуацию
            tokens = re.findall(r'\w+|[.,!?;:\-—]', sentence, re.UNICODE)
            self.tokens.extend(tokens)
        
        # Подсчитываем частоту слов
        self.vocabulary = Counter([t.lower() for t in self.tokens if t.isalnum()])
        return self
    
    def get_sentences(self):
        """Получить список предложений"""
        return self.sentences
    
    def get_tokens(self):
        """Получить список токенов"""
        return self.tokens
    
    def get_vocabulary(self, top_n=10):
        """Получить частые слова"""
        return self.vocabulary.most_common(top_n)
    
    def get_statistics(self):
        """Получить статистику"""
        return {
            'sentences': len(self.sentences),
            'tokens': len(self.tokens),
            'unique_words': len(self.vocabulary),
            'avg_sentence_length': len(self.tokens) / len(self.sentences) if self.sentences else 0
        }


# Пример использования
if __name__ == "__main__":
    text = """Компьютерная лингвистика — это область науки, находящаяся на стыке 
    лингвистики и информатики. Она изучает проблемы автоматической обработки текстов. 
    Главная цель — научить компьютер понимать и генерировать человеческую речь."""
    
    processor = TextProcessor()
    processor.load_text(text).segment_sentences().tokenize()
    
    print("=== ПРЕДЛОЖЕНИЯ ===")
    for i, sent in enumerate(processor.get_sentences(), 1):
        print(f"{i}. {sent}")
    
    print("\n=== ТОКЕНЫ ===")
    print(processor.get_tokens())
    
    print("\n=== ТОП-10 СЛОВ ===")
    for word, freq in processor.get_vocabulary(10):
        print(f"{word}: {freq}")
    
    print("\n=== СТАТИСТИКА ===")
    stats = processor.get_statistics()
    for key, value in stats.items():
        print(f"{key}: {value:.2f}" if isinstance(value, float) else f"{key}: {value}")
```



## Часть 3: Практические упражнения

### Упражнение 1: Базовая токенизация

**Задача:** Реализовать функцию, которая разделяет текст на слова, учитывая пробелы и табуляции.

```python
def exercise_1(text):
    """
    TODO: Реализовать простую токенизацию по пробелам
    
    Требования:
    - Разделить текст по пробелам, табуляциям и переводам строк
    - Удалить пустые токены
    - Вернуть список токенов
    """
    pass


# Тест
test_text = "Привет   мир!\tЭто\nтест"
expected = ["Привет", "мир!", "Это", "тест"]
# assert exercise_1(test_text) == expected
```

**Подсказка:** Используйте методы строк `.split()` или регулярные выражения.



### Упражнение 2: Разделение пунктуации

**Задача:** Создать функцию, которая отделяет пунктуацию от слов.

```python
def exercise_2(text):
    """
    TODO: Токенизировать текст с отделением пунктуации
    
    Требования:
    - Отделить знаки пунктуации (. , ! ? ; :) от слов
    - Сохранить дефисы внутри слов (например: "какой-то")
    - Вернуть список токенов
    
    Пример:
    "Привет, мир!" -> ["Привет", ",", "мир", "!"]
    """
    pass


# Тест
test_cases = [
    ("Привет, мир!", ["Привет", ",", "мир", "!"]),
    ("Какой-то текст.", ["Какой-то", "текст", "."]),
    ("Это вопрос?", ["Это", "вопрос", "?"])
]
```

**Подсказка:** Используйте регулярные выражения с методом `re.findall()`.



### Упражнение 3: Простая сегментация предложений

**Задача:** Разделить текст на предложения по границам.

```python
def exercise_3(text):
    """
    TODO: Разделить текст на предложения
    
    Требования:
    - Разделить по точкам, вопросительным и восклицательным знакам
    - Удалить пустые предложения
    - Убрать лишние пробелы в начале и конце
    - Вернуть список предложений
    
    Пример:
    "Первое. Второе?" -> ["Первое", "Второе"]
    """
    pass


# Тест
text = "Компьютер - помощник человека. Он решает задачи. Какие задачи?"
# expected = ["Компьютер - помощник человека", "Он решает задачи", "Какие задачи"]
```



### Упражнение 4: Обработка аббревиатур

**Задача:** Разработать функцию, которая правильно обрабатывает аббревиатуры при сегментации.

```python
def exercise_4(text, abbreviations):
    """
    TODO: Сегментация с поддержкой аббревиатур
    
    Args:
        text (str): Исходный текст
        abbreviations (list): Список известных аббревиатур
        
    Требования:
    - Учитывать аббревиатуры при разделении
    - Не разбивать предложение на границе аббревиатуры
    - Вернуть список предложений
    
    Пример:
    text = "Получено 1.5 млн. рублей. Это сумма!"
    abbreviations = ['млн.', 'млрд.', 'тыс.']
    expected = ["Получено 1.5 млн. рублей", "Это сумма"]
    """
    pass
```



### Упражнение 5: Подсчет статистики

**Задача:** Написать программу для подсчета статистики по токенам и предложениям.

```python
def exercise_5(text):
    """
    TODO: Подсчитать статистику текста
    
    Требования:
    - Разделить на предложения
    - Разделить на токены
    - Подсчитать:
      * Количество предложений
      * Количество слов (токенов без пунктуации)
      * Количество уникальных слов
      * Среднюю длину предложения в словах
      * Топ-5 самых частых слов
    
    Вернуть словарь с результатами:
    {
        'sentences_count': int,
        'words_count': int,
        'unique_words': int,
        'avg_sentence_length': float,
        'top_5_words': list of tuples (word, frequency)
    }
    """
    pass


# Тест
text = "Данные - это новое золото. Данные помогают принимать решения. Анализ данных важен!"
```



### Упражнение 6: Фильтрация стоп-слов

**Задача:** Удалить из текста стоп-слова (служебные слова).

```python
def exercise_6(text, stopwords=None):
    """
    TODO: Токенизировать и удалить стоп-слова
    
    Требования:
    - Токенизировать текст
    - Удалить стоп-слова (предлоги, союзы, артикли и т.д.)
    - Сохранить пунктуацию
    - Вернуть список значимых токенов
    
    Стоп-слова по умолчанию:
    и, а, о, в, на, с, из, к, по, у, для, от, как, что, это, 
    те, та, то, то, но, же, ли, ты, я, он, она, оно, они, мы, вы
    """
    if stopwords is None:
        stopwords = {'и', 'а', 'о', 'в', 'на', 'с', 'из', 'к', 'по', 'у', 
                     'для', 'от', 'как', 'что', 'это', 'но', 'же', 'ли'}
    pass
```



### Упражнение 7: Класс для обработки текста (ПРОДВИНУТОЕ)

**Задача:** Создать класс `AdvancedTextProcessor` со следующими методами:

```python
class AdvancedTextProcessor:
    """
    TODO: Реализовать полнофункциональный процессор текста
    
    Методы:
    - __init__(language='russian')
    - load_text(text) -> self (для цепочки вызовов)
    - segment() -> self
    - tokenize() -> self
    - normalize() -> self (приведение к нижнему регистру, удаление диакритики)
    - remove_stopwords(stopwords_list) -> self
    - get_sentences() -> list
    - get_tokens() -> list
    - get_unique_words() -> set
    - get_word_frequencies() -> dict
    - get_statistics() -> dict
    
    Пример использования:
    processor = AdvancedTextProcessor('russian')
    result = processor.load_text(text).segment().tokenize().get_statistics()
    """
    pass
```



### Упражнение 8: Обработка специальных случаев (ИССЛЕДОВАТЕЛЬСКОЕ)

**Задача:** Обработать следующие случаи в текстах:

```python
def exercise_8_handle_special_cases():
    """
    TODO: Разработать функции для обработки:
    
    1. Веб-адреса: https://example.com, www.site.ru
    2. Электронная почта: user@example.com
    3. Хештеги: #компьютернаялингвистика
    4. Упоминания: @username
    5. Цифры и числа: 1000, 3.14, 1,000.50
    6. Эмодзи: 😊 👍
    7. URL в скобках: (https://example.com)
    8. Цены: $100, €50, ₽1000
    
    Создать функцию tokenize_with_special_handling(text)
    которая корректно обрабатывает эти случаи.
    """
    pass


# Тест
test_texts = [
    "Посетите https://example.com для подробной информации.",
    "Свяжитесь с нами: info@company.ru",
    "Новый пост #компьютернаялингвистика был популярен! 👍",
    "Цена составляет $100 или €85.",
    "Результат: 3.14 * 100 = 314"
]
```




## Контрольные вопросы

### Вопросы для проверки понимания

1. **Что такое токенизация и в чем разница между символьной и словной токенизацией?**

2. **Назовите три типа задач, где требуется разделение текста на предложения и слова.**

3. **Какие проблемы возникают при сегментации предложений в русском языке?**

4. **Приведите примеры аббревиатур, которые требуют специальной обработки при сегментации.**

5. **Какие различия между пунктуацией в начале, конце и внутри слова?**

6. **Объясните, почему простое разделение по пробелам недостаточно для полноценной токенизации.**

7. **Какие данные нужны для обучения модели сегментации предложений?**

8. **Можно ли использовать одну и ту же модель токенизации для русского и английского языков? Почему?**

9. **Что такое стоп-слова и зачем их удаляют из текста?**

10. **Описать различие между лемматизацией и токенизацией.**



### Практические вопросы

11. **Напишите регулярное выражение для выделения всех URL в тексте.**

12. **Как обработать случай, когда названия собственные содержат внутренний пробел ("Нью-Йорк", "Санкт-Петербург")?**

13. **Объясните использование маркеров при обработке аббревиатур (как в Примере 4).**

14. **Какие метрики можно использовать для оценки качества сегментации предложений?**

15. **Как учитывать контекст при разделении текста на слова (например, при обработке "не знаю")?**



### Вопросы для размышления

16. **Какие лингвистические явления делают задачу токенизации нетривиальной?**

17. **Можно ли создать универсальную программу для всех языков? Почему?**

18. **Как различаются подходы для письменной и устной речи?**

19. **Какую роль играет предварительная обработка текста в дальнейших задачах NLP?**

20. **Опишите, как вы бы разработали систему токенизации для малоизученного языка с ограниченными ресурсами.**



## Дополнительные ресурсы

### Библиотеки для обработки текста

**NLTK (Natural Language Toolkit)**
- Документация: https://www.nltk.org/
- Установка: `pip install nltk`
- Используется для базовых операций с текстом

**spaCy**
- Документация: https://spacy.io/
- Установка: `pip install spacy`
- Отличный выбор для производительности и качества

**TextBlob**
- Документация: https://textblob.readthedocs.io/
- Установка: `pip install textblob`
- Простой интерфейс для начинающих

**Pymorphy2** (для русского языка)
- Документация: https://pymorphy2.readthedocs.io/
- Установка: `pip install pymorphy2`
- Морфологический анализ и лемматизация

### Датасеты

- **Russian Corpus** - НКРЯ (Национальный корпус русского языка)
- **Universal Treebank** - многоязычные синтаксические корпусы
- **OpenCorpora** - открытый корпус русского языка с морфологической разметкой

### Дополнительная литература

- Jurafsky, D., & Martin, J. H. (2009). Speech and Language Processing
- Гладкий, А. В. (1985). Синтаксис и распознавание
- Кулев, С. А., & Мусатова, С. В. (2017). Компьютерная лингвистика



## Заключение

Программы для разделения слов и предложений — это фундамент для всей дальнейшей обработки текста. Умение правильно реализовать токенизацию и сегментацию критически важно для специалистов в области компьютерной лингвистики.

На этом уроке мы изучили:
- Теоретические основы токенизации и сегментации
- Различные подходы и алгоритмы
- Готовые библиотеки и инструменты
- Практические примеры кода
- Расширенные методы обработки